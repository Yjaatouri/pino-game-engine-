# Helper: simple example without assets
function(add_pino_example name)
    add_executable(${name} ${name}/main.cpp)
    target_link_libraries(${name} PRIVATE engine_core)
endfunction()

# Helper: example with assets (engine resolves asset root at runtime)
function(add_pino_example_assets name)
    add_executable(${name} ${name}/main.cpp)
    target_link_libraries(${name} PRIVATE engine_core)
endfunction()

# ─── Simple examples (no assets) ───────────────────────────────
add_pino_example(triangle)
add_pino_example(clear)
add_pino_example(cube)
add_pino_example(fps_camera)
add_pino_example(scene_graph)
add_pino_example(entity_test)
add_pino_example(scene_graph_test)
add_pino_example(component_test)
add_pino_example(scene_lifecycle_test)
add_pino_example(prefab_test)
add_pino_example(debug_overlay_test)
add_pino_example(ecs_inspector_test)
add_pino_example(prefab_debug_viewer_test)
add_pino_example(debug_console_test)
add_pino_example(profiler_test)
add_pino_example(stage3_validation_test)
add_pino_example(physics_sync_test)
add_pino_example(physics_mapping_test)
add_pino_example(destroy_stress_test)
add_pino_example(serialization_stall_test)
add_pino_example(batch_render_test)
add_pino_example(cooked_asset_test)

# serialization_stall_test uses std::thread (part of C++17, no extra libs needed)

# ─── Asset-using examples ─────────────────────────────────────
add_pino_example_assets(asset_demo)
add_pino_example_assets(lit_scene)
add_pino_example_assets(game_demo)
add_pino_example_assets(input_test)
add_pino_example_assets(collision_demo)
add_pino_example_assets(arena_game)
add_pino_example_assets(demo_game)
add_pino_example_assets(stability_test)
target_compile_definitions(stability_test PRIVATE _CRT_SECURE_NO_WARNINGS)
add_pino_example_assets(audio_test)
add_pino_example_assets(collision_bench)
add_pino_example_assets(micro_arena)
